# Completion matrix

This file is the release audit for StarConverter. A green unit-test suite is necessary but is not
evidence that the converter is ready. Every row must have direct, current evidence before a release
may claim image conversion or physical-volume support.

| Requirement | Current evidence | Status / remaining proof |
| --- | --- | --- |
| Raw-device safety boundary | `image.rs` accepts regular files only and lexically refuses device namespaces; workspace forbids unsafe Rust | Proven for current read-only build; physical backend remains absent by design |
| exFAT read/inventory | Boot-region checksums, FAT/bitmap pairing, directory/upcase parsing, recursive ownership inventory, mutation/fuzz tests, and empty plus populated exfatprogs/fuse-exfat images; exact legacy nearest-rounded `PercentInUse` is bounded while the bitmap remains authoritative; logical bitmap length, surplus bytes, and unused tail bits are checked exactly | Implemented for bounded image files; bitmap bytes/tail states that the current escrow cannot preserve are refused, and specialized features plus broader real-world corpus remain |
| NTFS read/inventory | Boot backup, MFT/MST, record-1 `$DATA` ownership of the boot-declared `$MFTMirr`, complete source runlist/allocation proof, including same-record `$MFT` `$DATA` VCN stitching and resident or non-resident (VCN-split) record-zero `$ATTRIBUTE_LIST` follow, NTFS-3G-compatible complete-record short-mirror handling, full geometry-derived `$MFT` initialization proof, and exact repaired used-FILE-content comparison through reserved record 15; Windows-compatible stale later records and unused tails are ignored but malformed compared-record MST/FILE structure fails closed. Attributes/lists, runlists, volume bitmap, indexes, recursive inventory and normalization follow; a mirror mismatch or incomplete proof prevents clean health evidence. Every nonresident physical attribute run is reconciled against the retained canonical `$Bitmap`, with claimed-free, overlapping, and (for complete inventory) unexplained allocation refused; bounded exact `$MFT::$BITMAP` evidence is separately reconciled against every scanned FILE in-use flag and rejects set bits beyond initialized `$MFT`; populated NTFS-3G images cover legal unused-record, directory-attribute, metadata-record, root-self-index, and stale `$FILE_NAME` cache differences; DOS 8.3 `$FILE_NAME` companions stay in the sidecar and do not inflate the dest-native graph hard-link count | Implemented for bounded image files; complete mirror-allocation ownership is a deliberate StarConverter conversion-authority gate beyond NTFS-3G mount validation. Incomplete inventory never claims exact reconciliation, `$MFT` unnamed `$DATA` same-record fragments and resident or non-resident (VCN-split) `$ATTRIBUTE_LIST` hosts inside the growing map are concatenated, `$MFT::$BITMAP`, `$MFTMirr::$DATA`, `$Volume::$VOLUME_INFORMATION`, and `$Bitmap::$DATA` are resolved through their records' `$ATTRIBUTE_LIST` (resident or non-resident) with VCN-split extents concatenated in order, continuation hosts outside the decoded `$MFT` map remain explicit incomplete evidence, and Windows-origin plus specialized-feature corpus remains |
| Neutral semantic model | `object.rs`, `extent.rs`, exFAT/NTFS normalization sidecars, 25-field preservation policy, and policy-bound bidirectional structural adapters | Common subset and escrow-bound metadata/identity mapping implemented; executable activation remains blocked by the serializer rows below |
| Destination geometry | Deterministic reservations/relocations with separate source-I/O and destination-cluster alignment, an exact target allocation domain, exact source→semantic-projection→relocated-target graph proof, durable distinct source/final commitments, overlap/cap/adversarial tests, and sealed stream-level materialization of resident NTFS payloads, destination-unaligned fragments, and dest-aligned runs whose initialized bytes leave destination slack, proven in both create-new directions | Core relocation authority, target-metadata regeneration, and copy-export materialization are integrated in both create-new directions. In-place serializer qualification remains |
| exFAT serializer | Complete boot/FAT/bitmap/upcase/directory candidate with exact metadata/profile inputs, specification-mandated `F4` non-bootable formatter code, independent generated-boot self-validation, exact Microsoft/exfatprogs recommended Up-case profile, parser round trips, current clean `fsck.exfat -n`, and read-only FUSE payload evidence; a typed draft exposes no writes, pins target geometry plus bitmap/upcase/directory allocations, marks only file data movable, and final serialization regenerates FAT, bitmap, directory first-cluster fields, `NoFatChain`, and `PercentInUse` while rejecting graph/layout substitution; source-absolute bad-cluster ranges become dest-heap FAT `0xFFFFFFF7` marks, allocation-bitmap bits, and solver reservations when they land entirely in the heap | Relocation-aware structural subset implemented; unsupported benign raw-entry/nonzero-padding sources are refused, and a bad-cluster mark that would occupy dest metadata or leave the heap fails closed. Activation remains blocked until per-candidate driver evidence is bound into authorization and Windows evidence passes |
| NTFS serializer | Boot, MFT/bitmap, volume, bounded resident or spilled `$I30` with internal `INDX` levels when the resident root cannot hold every leaf separator, independently validated cluster- or sector-based VCN geometry through 64 KiB clusters, system namespace, exact object metadata, pinned Windows-6.1 `$UpCase`, canonical `$AttrDef`, `$BadClus:$Bad` as a volume-sized virtual stream with identity-mapped physical runs at known-bad dest LCNs (empty when none), matching `$Bitmap` bits and solver reservations, pinned clean `$LogFile`, pinned `$Secure` plus explicit ordinary-object `0x101` security IDs, and typed `$Extend` bootstrap; a typed layout draft exposes no writes, permits only file-data relocation, and final serialization proves invariant reservations while regenerating runlists and `$Bitmap`; dest-graph `BadCluster` extents remain refused in favor of volume-profile ranges; ordinary files may carry multiple `$FILE_NAME` hard links and named `$DATA` (resident, dest-cluster-materialized resident overflow, or cluster-aligned non-resident); unnamed `$DATA` may be sparse with cluster-aligned holes, the `$DATA` sparse flag, and a 72-byte non-resident header; `$DATA` mapping pairs that cannot fit one empty FILE record become VCN-contiguous continuation extents whose first extent keeps whole-stream sizes; a file whose attributes overflow one 1024-byte FILE record emits a `$ATTRIBUTE_LIST` and in-use extension records that independent inventory resolves, dest-cluster-materializing a non-resident list when `$STANDARD_INFORMATION` plus a resident list cannot fit the base record, and splitting a file or directory list that needs two or more clusters into a one-cluster first extent plus a VCN-contiguous continuation; directories may carry multiple `$FILE_NAME` hard links but not named streams, and a directory whose names leave no room for `$INDEX_ROOT` (or a spilled `$INDEX_ALLOCATION`) likewise emits a `$ATTRIBUTE_LIST` with extra names in extension records, including a dest-cluster-materialized non-resident list when the resident form cannot sit beside `$INDEX_ROOT`; resident `$REPARSE_POINT` payloads are emitted on files and directories with the reparse attribute bit while `$INDEX_ROOT` budgeting reserves the suffix, and every emitted reparse point is listed exactly once in the `$Extend\$Reparse:$R` view index (32-byte `REPARSE_INDEX` entries keyed by tag plus FILE reference in `COLLATION_NTOFS_ULONGS` order); when the keys overflow the resident `$Reparse` root, `ntfs_reparse_index` spills them into 4 KiB `INDX` leaves with internal `INDX` levels as needed, emits `$INDEX_ALLOCATION:$R` in dest metadata clusters after the `$I30` spills plus a resident `$BITMAP:$R`, budgets the separator root against the record's real attribute bytes, and independently walks root and blocks (MST repair, canonical headers, child-VCN reachability, strict collation) before `ntfs_extend` accepts the typed metadata; a 301-reparse-point candidate (three leaves) inventories cleanly and pins identical geometry through draft and finalize; the read-only inventory independently locates `$Extend\$Reparse` by name, walks its resident or spilled `$R` (lenient MST-repaired `INDX` readers that accept foreign LSN/USN/unused bytes), and reconciles every key against the `$REPARSE_POINT` census (reference, sequence, and tag both ways), failing closed on stale, unlisted, mismatched, or duplicate keys and on a missing or duplicate `$Reparse` metafile, with the outcome surfaced as `NtfsInventory::reparse_index` and in `inspect-image` | Relocation-aware structural subset implemented and exercised with a conflicting live exFAT payload. Overlap between a bad mark and dest metadata or final file payload fails closed. `$MFT` `$ATTRIBUTE_LIST` continuation hosts beyond the first-extent map, compressed/encrypted dest streams, modern-native LogFile/Secure/Extend proof, `$Extend` case semantics, `$UsnJrnl`/`$RmMetadata`, and Windows validation remain activation blockers |
| Activation authorization | The final serializer authorization type is opaque; preflight, observation, phase-completion, staging/final verification, and rollback-completion fields are module-private, with external preflight forgery compile-failing; raw unleased executor mutators are private; a one-use Windows preparation session mints its transaction identity internally, inspects, hashes, manifests, plans, and captures preimages through one mandatory deny-share locked handle, rejects substituted preflight/graph/rollback evidence, and consumes full capsule namespace durability before producing the coordinator; crate-private leases then bind mutation to the exact capsule generation, phase, plan, container, and both flush barriers | External and stale-intent mutation bypasses are sealed. Locked regular-image coordination through `Activated`, separate verification, rollback through `Verified`, and test-only capability-gated finalization are implemented. Advisory-only hosts fail closed before offline evidence is minted. Serializer qualification and frontend acceptance policy remain blocked |
| Transaction capsule | Redundant headers, generation/phase validation, a canonical bounded `SCPREP02` generation-zero plan envelope with distinct source/relocated-target graph commitments and nested `SCRECOV2` exact relocation/phase before-images, torn-tail prefix recovery, plus an exclusively locked regular-file store that validates before create/resume, permits exactly one append-only generation at a time, flushes and reads back every suffix, and rechecks file identity/path/length; successful creation additionally flushes the canonical parent namespace on Unix and Windows, with rejected/unsupported directory flushes returning insufficient evidence after leaving the verified file recoverable | Complete regular-image authority through activation can be reconstructed from capsule plus locked image after process loss; envelope truncation, tamper, noncanonical order, cap, commitment, recovery mismatch, wrong identity, complete corruption, and ambiguous multi-generation growth fail closed. Version-1 executable envelopes are refused; legacy envelope-free recovery capsules are external-plan/rollback-only. Windows and Unix namespace barriers are implemented; stronger-than-advisory Unix image exclusion remains unavailable |
| Crash rollback | Exact relocation-destination and phase before-images; conservative coverage of the possibly in-flight next mutation; finalize is the rollback boundary | Every relocation copy boundary is retryable; partial copies under a durable `Reserved` checkpoint converge, relocated bytes are re-compared before staging after restart, and exact rollback restores the original image. Executor write/sync cuts plus capsule `BeforeWrite`, `AfterWrite`, `AfterSyncData`, `AfterReadback`, `AfterSyncAll`, and `BeforeAdopt` matrices cover creation and later phase seams against flushed regular files; broader cross-platform and eventual physical-backend fault execution remain |
| Image mutation executor | Regular-image-only executor with exclusive locking, fixed-size identity checks, a domain-separated plan-to-container token, bounded relocation/write chunks, read-after-write verification, flush barriers, rollback, deterministic fault injection, opaque completion values, and no public raw mutation API; its lifetime-bound bounded read view clones the already-open locked handle without namespace reopen; an internal preparation session and coordinator retain image-then-capsule lock ownership from trusted evidence construction through initial durable checkpoint, reconstruct an owned plan from generation zero, persist image before capsule, reconcile restart bytes, verify source-equals-relocation-destination before staging, and support durable rollback through `Verified`; full real-byte/parser/graph/manifest audits guard verification and capability-gated finalization | Mandatory Windows regular-image preparation and locked coordination through activation are implemented with authority-substitution, relocation corruption/partial-cut, torn-write, restart, and rollback regressions. Qualified relocation-aware serializers, mandatory non-Windows exclusion, frontend acceptance authority, and independent target validation remain required before any in-place claim |
| Copy-based image exporter | Create-new-only regular output, exact preview/source binding, a pre-planning whole-image SHA-256/container snapshot, opaque source-graph→layout→derived-target authority, pinned-handle candidate verification, restrictive/locked partial construction, full source copy, sealed payload relocation and stream materialization before metadata, virtual-source expected-manifest construction, target reinspection, namespace/content manifest equality, source/candidate/manifest/direction-bound schema-v4 escrow sidecar, and before/after source SHA-256 | Relocation-aware in both directions. Resident NTFS payloads, destination-unaligned fragments, dest-aligned runs with uninitialized tails, and escrow NTFS sparse holes are sealed-materialized (not 1:1 copied) on create-new export so destination slack and holes are zeros rather than source cluster residue; mixed 1:1 relocation plus resident materialization shares one RelocatePayload progress total; escrow NTFS hard-link groups (including directory hard links and cross-directory file names) and named streams (resident or cluster-aligned non-resident) export as dest-native exFAT names and unnamed payloads with extra identities retained in the sidecar; named streams the sidecar cannot capture (above the inventory capture cap, sparse, or LZNT1) travel as hidden+system dest-native carrier files under `\.starconverter-escrow` through the same sealed relocation/materialization path, and a source root entry colliding with that reserved name refuses the export; escrow NTFS sibling names that collide under the recommended exFAT up-case table keep one original dest-native name and rename the rest with a `~N` suffix while originals stay in escrow; illegal dest names still refuse; escrow NTFS `$REPARSE_POINT` payloads export as dest-native exFAT files without reparse semantics while exact attribute bytes stay in the inner NTFS snapshot; NTFS DOS 8.3 `$FILE_NAME` companions stay in escrow and do not count as dest-native hard links; escrow NTFS LZNT1 `$DATA` streams decompress into dest-native unnamed payloads (destination slack zeros) rather than copying compressed clusters, while the exact compressed mapping and compression-unit stay in inner NTFS snapshot v7; and the source is left byte-identical. Stale payload edits and cancellation during relocation or materialization fail before publication with no leaked partial. A valid exFAT image whose payload conflicts with NTFS metadata and a valid NTFS image whose 8 KiB payload is misaligned for the future exFAT heap remain independently relocated. Publication is atomic no-clobber but currently requires hard-link support; Unix synchronizes parent namespaces and Windows reports directory durability as unsupported. Inner NTFS snapshot v7 restores extra non-DOS hard links, resident named streams, captured nonempty non-resident named streams, and resident file and directory `$REPARSE_POINT` payloads by dest-native path (including remapped dest ObjectIds), and rematerialized reparse points are listed in `$Extend:$R`; the exFAT→NTFS convert path (CLI `convert-image --restore-escrow`, GUI restore-escrow field) consumes the candidate-bound NTFS→exFAT sidecar only after the envelope direction and the source exFAT image SHA-256 match, then restores exact `$STANDARD_INFORMATION` timestamps/attributes, volume serial, and label alongside the identities; a mismatched or wrong-direction sidecar is refused before any planning |
| Cross-format adapters | exFAT→NTFS exact DOS metadata, calendar/offset-to-FILETIME conversion, serial/label mapping, pinned layout draft and solved final graph; NTFS→exFAT bounded name/collision proof, FILETIME rounding, canonical Up-case profile, serial/label policy, pinned exFAT layout draft and solved final graph; exact source-only evidence and proven pinned `$Secure:$SDS` bytes retained in schema-v4 escrow; non-object source metadata becomes a staging exclusion rather than relocation scratch; consistent exFAT bad-cluster extents and NTFS `$BadClus:$Bad` physical runs project as dest-native unusable clusters (NTFS identity `$Bad`/`$Bitmap`, exFAT FAT `0xFFFFFFF7`/bitmap) with the exact source runlist remaining in escrow; escrow NTFS sparse files lose the sparse flag on the dest graph and materialize holes as zeros; escrow NTFS LZNT1 streams lose the dest compressed flag, keep the compression-unit on the projected graph so reconstruct cannot copy compressed clusters as plaintext, and materialize decompressed dest-aligned extents with the CU cleared on the solved dest graph; Strict still refuses Compression; escrow NTFS sibling names that collide under the recommended exFAT up-case table are dest-native disambiguated; escrow NTFS `$REPARSE_POINT` objects lose dest reparse semantics while exact unnamed resident payload bytes remain in the inner NTFS snapshot; NTFS DOS 8.3 `$FILE_NAME` companions are sidecar aliases rather than dest-native hard links | Both structural directions and both relocation-aware create-new CLI/desktop paths are implemented. Escrow NTFS→exFAT now projects file and directory hard-link groups to one dest-native name and drops named streams (resident or non-resident) from the dest graph while retaining exact names and stream bytes in schema-v4 escrow, or, when the sidecar cannot capture the bytes, carries them as dest-native `\.starconverter-escrow` files whose identity is derived from the escrowed record and attribute numbers; colliding dest-native names keep the lowest ObjectId unchanged and suffix the rest with `~N` so the exFAT writer never sees a folded collision; illegal names and Strict mode still refuse NamesAndCase. Incomplete or non-resident `$REPARSE_POINT` mappings still refuse; complete resident payloads are EscrowRequired and dest-native exFAT files. DOS 8.3 companions no longer inflate HardLinks; real multi-name groups still escrow. Inconsistent bad-cluster count/extent evidence fails closed. Escrow restore matches dest-native paths so remapped inventories still reattach extra non-DOS `$FILE_NAME`s, resident named `$DATA`, captured nonempty non-resident named `$DATA`, and resident file and directory `$REPARSE_POINT` bytes; exact source MFT numbers/runlists are not rematerialized, rematerialized reparse points populate `$Extend:$R` (resident, or spilled into `INDX` records once the root overflows), the read-only inventory reconciles that listing against the `$REPARSE_POINT` census on the resulting candidate, and a full NTFS→exFAT(+escrow)→NTFS round trip is proven in-tree to restore hard links, named-stream bytes, both reparse payloads, exact timestamps/attributes, serial, label, and the `$Extend:$R` listing. In-place activation remains blocked until target serializer and independent platform qualification gates close |
| Independent verification | Overlay parser/normalizer plus namespace/content manifests; manifest construction refuses compressed or encrypted extent storage before issuing any raw payload read; reproducible root, rich, edge, empty-formatter, and populated-formatter corpus; actual converted LBA-2048 fixed-VHD fixtures; read-only external results and a bounded, fail-closed schema-v1 Windows report verifier in `EXTERNAL_VALIDATION.md` | Nested Unicode, maximum-name, allocation-boundary, large fragmented, exfatprogs/fuse-exfat, and NTFS-3G images pass independent read-only checks and StarConverter inspection without image-byte changes; create-new dest-native NTFS→exFAT decompresses LZNT1 before hashing the candidate, but independent source-extent hashing still refuses compressed or encrypted storage; a logical decryption reader, elevated read-only Windows `chkdsk`, detach, hash evidence, Windows-origin images, and specialized NTFS features remain |
| CLI workflow | Read-only `inspect`, exact blocked `preview`, create-new `convert-image`, read-only candidate-bound `verify-export`, strict non-authorizing `verify-windows-report`, synthetic `plan`, explicit policy/serializer blockers | In-place convert/rollback/finalize remain intentionally absent until a target serializer qualifies |
| Desktop workflow | Native egui shell, dark text-only ASCII identity with no logo asset, deterministic wide/medium/compact layouts, 44-point interaction targets, decorative ASCII excluded from AccessKit, and a stable Source→Direction→Guarantee→Preflight→Action→Activity accessibility group tree independent of visual panel paint order; regular-image picker/inspection, exact blocked preview, create-new image export, read-only bound-export verification, interrupted-export guidance, deterministic evidence reports, generation-tagged background jobs, byte-level coalesced progress, cooperative cancellation through safe checkpoints, truthful non-cancellable publication state, deferred close, and bounded session recovery with fail-closed stale/corrupt/device-path handling; source-path, direction, or guarantee edits immediately invalidate prior source/plan/preview/verification evidence and visibly mark the plan stale; keyboard semantics, palette contrast, 760-point/200%-scale layout, target size, headings, branding, and semantic ownership are regression-tested; in-place conversion disabled | Needs a native egui snapshot/capture harness, live NVDA/Narrator/VoiceOver/Orca and full installed-package keyboard audit, forced-colors and native 200% screenshot validation, cancellation qualification on very large images, and installed-package testing |
| Cross-platform gates | Current Rust + Rust 1.85 core/CLI, Rust 1.95 GUI/release builds, Go lab, parser/escrow fuzz CI; four-target portable archives plus two deterministic unsigned macOS `.app` layouts with exact-member/architecture/native dependency checks, Windows PE subsystem/manifest/VERSIONINFO/no-icon/unsigned checks, canonical inventories, SHA-256 manifest, CycloneDX SBOM identity checks, frozen provenance subjects, offline Sigstore verification, pinned release actions, default-deny permissions, scheduled RustSec/govulncheck scans, and Dependabot updates | Local hostile package-validator tests and real Windows PE resources pass; two isolated same-host Windows builds byte-match with target remapping and `/Brepro`. Every checkpoint requires a fresh full hosted CI/manual-release exercise; independent cross-runner reproducibility, native Authenticode/Developer-ID signing, notarization, Windows/Linux installers, and installed-package testing remain |
| Physical-volume support | None | Requires every gate in `SAFETY.md`, separate review, and later a uniquely identified sacrificial removable drive |
| Ship contract | Repository policy requires commit, `origin/main`, and 7z backup | Run only after the selected release milestone passes this matrix |

## Release labels

- **Copy-conversion pre-alpha:** supported sources may become a brand-new verified regular target;
  source mutation, overwrite, and devices remain unavailable.
- **In-place image-conversion alpha:** activation-ready strict subset, regular-image executor, full crash cuts,
  internal and external structural validation; no raw devices.
- **Physical preview:** every physical gate in `SAFETY.md`, platform lock/dismount/flush review, and
  sacrificial-media qualification.
- **Ready:** signed cross-platform UX, recovery tooling, compatibility matrix, documentation, and no
  unqualified losslessness claims.

The current repository is a **copy-conversion pre-alpha**. In-place activation remains blocked.
