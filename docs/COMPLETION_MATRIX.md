# Completion matrix

This file is the release audit for StarConverter. A green unit-test suite is necessary but is not
evidence that the converter is ready. Every row must have direct, current evidence before a release
may claim image conversion or physical-volume support.

| Requirement | Current evidence | Status / remaining proof |
| --- | --- | --- |
| Raw-device safety boundary | `image.rs` accepts regular files only and lexically refuses device namespaces; workspace forbids unsafe Rust | Proven for current read-only build; physical backend remains absent by design |
| exFAT read/inventory | Boot-region checksums, FAT/bitmap pairing, directory/upcase parsing, recursive ownership inventory, mutation/fuzz tests, and empty plus populated exfatprogs/fuse-exfat images; exact legacy nearest-rounded `PercentInUse` is bounded while the bitmap remains authoritative; logical bitmap length, surplus bytes, and unused tail bits are checked exactly | Implemented for bounded image files; bitmap bytes/tail states that the current escrow cannot preserve are refused, and specialized features plus broader real-world corpus remain |
| NTFS read/inventory | Boot backup, MFT/MST, attributes/lists, runlists, volume bitmap, indexes, recursive inventory and normalization; every nonresident physical attribute run is reconciled against the retained canonical `$Bitmap`, with claimed-free, overlapping, and (for complete inventory) unexplained allocation refused; bounded exact `$MFT::$BITMAP` evidence is separately reconciled against every scanned FILE in-use flag and rejects set bits beyond initialized `$MFT`; populated NTFS-3G images cover legal unused-record, directory-attribute, metadata-record, root-self-index, and stale `$FILE_NAME` cache differences | Implemented for bounded image files; incomplete inventory never claims exact reconciliation, `$MFT::$BITMAP` behind `$ATTRIBUTE_LIST` remains explicit incomplete evidence, and Windows-origin plus specialized-feature corpus remains |
| Neutral semantic model | `object.rs`, `extent.rs`, exFAT/NTFS normalization sidecars, 25-field preservation policy, and policy-bound bidirectional structural adapters | Common subset and escrow-bound metadata/identity mapping implemented; executable activation remains blocked by the serializer rows below |
| Destination geometry | Deterministic reservations/relocations and overlap/cap tests | Implemented; serializer metadata must be regenerated if future plans allow payload relocation |
| exFAT serializer | Complete boot/FAT/bitmap/upcase/directory candidate with exact metadata/profile inputs, exact Microsoft/exfatprogs recommended Up-case profile, parser round trips, clean `fsck.exfat -n`, and an independent read-only FUSE payload mount | Structural subset implemented; unsupported benign raw-entry/nonzero-padding and bad-cluster sources are refused, but activation remains blocked until per-candidate driver evidence is bound into authorization and Windows evidence passes |
| NTFS serializer | Boot, MFT/bitmap, volume, bounded resident/one-level spilled `$I30`, system namespace, exact object metadata, pinned Windows-6.1 `$UpCase`, canonical `$AttrDef`, empty `$BadClus:$Bad`, pinned clean `$LogFile`, pinned `$Secure` plus explicit ordinary-object `0x101` security IDs, and typed `$Extend` bootstrap | Structural subset implemented; modern-native LogFile/Secure/Extend proof, `$Extend` case semantics, `$UsnJrnl`/`$RmMetadata`, multi-level indexes and >4 KiB-cluster sub-cluster VCNs, and Windows validation remain activation blockers |
| Activation authorization | The final authorization type is opaque; preflight, observation, phase-completion, staging/final verification, and rollback-completion fields are module-private, with external preflight forgery compile-failing; raw unleased executor mutators are private; crate-private one-use leases bind mutation to the exact capsule generation, current phase, plan, container, and both flush barriers; staged target parsing and logical hashing use the plan-bound overlay over the same locked handle, after exact real-byte comparison, and match a durable source-manifest commitment | External and stale-intent mutation bypasses are sealed. Locked resume observation and durable preactivation restart authority are implemented; trusted production initial preflight/capsule creation, activation coordination, and serializer qualification remain blocked |
| Transaction capsule | Redundant headers, generation/phase validation, a canonical bounded `SCPREP01` generation-zero plan envelope with nested exact recovery bundle, torn-tail prefix recovery, plus an exclusively locked regular-file store that validates before create/resume, permits exactly one append-only generation at a time, flushes and reads back every suffix, and rechecks file identity/path/length; recovering resume durably discards only a provably incomplete newest append, rereads, strict-scans, and retries | Complete preactivation authority can be reconstructed from capsule plus locked image after process loss; envelope truncation, tamper, noncanonical order, cap, commitment, and recovery mismatch fail closed. Legacy recovery-only capsules are external-plan/rollback-only. Post-activation evidence checkpoints, Windows namespace durability for reboot-safe proof, and stronger-than-advisory Unix exclusion remain unfinished |
| Crash rollback | Exact before-images; conservative coverage of the possibly in-flight next write group; finalize is the rollback boundary | Deterministic image-file matrix and executor fault injection implemented; broader cross-platform fault execution remains |
| Image mutation executor | Regular-image-only executor with exclusive locking, fixed-size identity checks, a domain-separated plan-to-container token, bounded writes, read-after-write verification, flush barriers, rollback, deterministic fault injection, opaque completion values, and no public raw mutation API; its lifetime-bound bounded read view clones the already-open handle without namespace reopen; an internal coordinator acquires image then capsule locks, constructs virtual-original source observations, can reconstruct an owned plan from generation zero, refuses nonempty relocation before writes, leases exact generation/phase work, persists the image before its checkpoint, supports durable rollback, and cannot cross `TargetStaged`; staged real bytes, parsing, graph, and logical manifest are independently checked | Non-activating coordinator foundation, honest locked resume observation, standalone preactivation restart, and staged-candidate audit are implemented. Trusted initial preflight/capsule creation, relocation-aware serialization, activation coordination, cross-platform executor fault execution, and full independent target validation remain required before any in-place claim |
| Copy-based image exporter | Create-new-only regular output, exact preview/source binding, pinned-handle candidate verification, restrictive/locked partial construction, full source copy, candidate writes, target reinspection, namespace/content manifest equality, source/candidate/manifest/direction-bound schema-v4 escrow sidecar, and before/after source SHA-256 | Implemented and exercised in both directions; publication is atomic no-clobber but currently requires hard-link support, Unix synchronizes parent namespaces before and after partial cleanup, and Windows reports directory durability as unsupported; structured collision/cleanup/sync failures retain exact recovery paths; rich and ten-payload edge candidates pass independent read-only checkers and FUSE mounts; elevated Windows validation remains |
| Cross-format adapters | exFAT→NTFS exact DOS metadata, calendar/offset-to-FILETIME conversion, serial/label mapping; NTFS→exFAT bounded name/collision proof, FILETIME rounding, canonical Up-case profile, serial/label policy; exact source-only evidence and proven pinned `$Secure:$SDS` bytes retained in schema-v4 escrow; non-object source metadata becomes a staging exclusion rather than relocation scratch | Both pure structural directions implemented and current/Rust-1.85 tested; neither can authorize activation until its target serializer qualifies |
| Independent verification | Overlay parser/normalizer plus namespace/content manifests; reproducible root, rich, edge, empty-formatter, and populated-formatter corpus; actual converted LBA-2048 fixed-VHD fixtures; read-only external results and a bounded, fail-closed schema-v1 Windows report verifier in `EXTERNAL_VALIDATION.md` | Nested Unicode, maximum-name, allocation-boundary, large fragmented, exfatprogs/fuse-exfat, and NTFS-3G images pass independent read-only checks and StarConverter inspection without image-byte changes; elevated read-only Windows `chkdsk`, detach, hash evidence, Windows-origin images, and specialized NTFS features remain |
| CLI workflow | Read-only `inspect`, exact blocked `preview`, create-new `convert-image`, read-only candidate-bound `verify-export`, strict non-authorizing `verify-windows-report`, synthetic `plan`, explicit policy/serializer blockers | In-place convert/rollback/finalize remain intentionally absent until a target serializer qualifies |
| Desktop workflow | Native egui shell, dark text-only ASCII identity with no logo asset, deterministic wide/medium/compact layouts, 44-point interaction targets, decorative ASCII excluded from AccessKit, and a stable Source→Direction→Guarantee→Preflight→Action→Activity accessibility group tree independent of visual panel paint order; regular-image picker/inspection, exact blocked preview, create-new image export, read-only bound-export verification, interrupted-export guidance, deterministic evidence reports, generation-tagged background jobs, byte-level coalesced progress, cooperative cancellation through safe checkpoints, truthful non-cancellable publication state, deferred close, and bounded session recovery with fail-closed stale/corrupt/device-path handling; keyboard semantics, palette contrast, 760-point/200%-scale layout, target size, headings, branding, and semantic ownership are regression-tested; in-place conversion disabled | Needs live NVDA/Narrator/VoiceOver/Orca and full installed-package keyboard audit, forced-colors and native 200% screenshot validation, cancellation qualification on very large images, and installed-package testing |
| Cross-platform gates | Current Rust + Rust 1.85 core/CLI, Rust 1.95 GUI/release builds, Go lab, parser/escrow fuzz CI; four-target portable archives plus two deterministic unsigned macOS `.app` layouts with exact-member/architecture/native dependency checks, Windows PE subsystem/manifest/VERSIONINFO/no-icon/unsigned checks, canonical inventories, SHA-256 manifest, CycloneDX SBOM identity checks, frozen provenance subjects, offline Sigstore verification, pinned release actions, default-deny permissions, scheduled RustSec/govulncheck scans, and Dependabot updates | Local hostile package-validator tests and real Windows PE resources pass; two isolated same-host Windows builds byte-match with target remapping and `/Brepro`. Every checkpoint requires a fresh full hosted CI/manual-release exercise; independent cross-runner reproducibility, native Authenticode/Developer-ID signing, notarization, Windows/Linux installers, and installed-package testing remain |
| Physical-volume support | None | Requires every gate in `SAFETY.md`, separate review, and later a uniquely identified sacrificial removable drive |
| Ship contract | Repository policy requires commit, `origin/main`, and 7z backup | Run only after the selected release milestone passes this matrix |

## Release labels

- **Copy-conversion pre-alpha:** supported sources may become a brand-new verified regular target;
  source mutation, overwrite, and devices remain unavailable.
- **In-place image-conversion alpha:** activation-ready strict subset, regular-image executor, full crash cuts,
  internal and external structural validation; no raw devices.
- **Physical preview:** every physical gate in `SAFETY.md`, platform lock/dismount/flush review, and
  sacrificial-media qualification.
- **Ready:** signed cross-platform UX, recovery tooling, compatibility matrix, documentation, and no
  unqualified losslessness claims.

The current repository is a **copy-conversion pre-alpha**. In-place activation remains blocked.
